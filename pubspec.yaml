name: e_straznik
description: "e-Strażnik — natywna aplikacja mobilna (kompanion read-only do web SaaS monitorującego liczniki Tauron eLicznik)."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Supabase
  supabase_flutter: ^2.8.0

  # State management
  flutter_riverpod: ^2.6.1

  # Routing
  go_router: ^14.6.2

  # Charts
  fl_chart: ^0.69.2

  # Secure storage / cache
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.3

  # Fonts
  google_fonts: ^6.2.1

  # i18n & formatting
  intl: ^0.20.2

  # Connectivity
  connectivity_plus: ^6.1.0

  # Firebase / push
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  firebase_crashlytics: ^4.2.0
  flutter_local_notifications: ^18.0.1

  # Deep links
  app_links: ^6.3.2

  # Pull-to-refresh z customowym widgetem (maskotka zamiast spinnera)
  custom_refresh_indicator: ^4.0.1

  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Generuje natywne splash screeny (iOS LaunchScreen + Android 12 splash)
  flutter_native_splash: ^2.4.3
  # Generuje ikony aplikacji dla iOS i Androida
  flutter_launcher_icons: ^0.14.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/

# Konfiguracja ikon aplikacji — uruchom:
#   dart run flutter_launcher_icons
# po każdej zmianie maskotki lub konfiguracji.
flutter_launcher_icons:
  ios: true
  android: "ic_launcher"
  remove_alpha_ios: true
  background_color_ios: "#1B2A4E"
  image_path: "assets/icons/app_icon.png"
  # Android adaptive icons (Android 8+)
  adaptive_icon_background: "#1B2A4E"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"
  min_sdk_android: 21

# Konfiguracja natywnego splasha — uruchom:
#   dart run flutter_native_splash:create
# po każdej zmianie tej sekcji lub maskotki.
flutter_native_splash:
  color: "#1B2A4E"
  image: assets/images/mascot.png
  android_12:
    color: "#1B2A4E"
    image: assets/images/mascot.png
    icon_background_color: "#1B2A4E"
  ios: true
  android: true
  web: false
  fullscreen: false
